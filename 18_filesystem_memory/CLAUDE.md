# 專案記憶（CLAUDE.md）

> 這個檔案在 `setting_sources` 含 `"project"` 時會被自動載入，成為 agent 的長期記憶。
> 它就是第 18 課的「教材」——`main.py` 問的問題，答案都寫在這裡。

## 關於本專案

- 本專案的吉祥物是一隻叫「阿露」的水獺 🦦。
- 專案代號：`claude-agent-class`。

## 程式碼風格規則

- 一律使用繁體中文撰寫註解。
- 每個函式都要加上 type hint。
- 縮排使用 4 個空格，不使用 tab。
- 字串優先使用雙引號。
